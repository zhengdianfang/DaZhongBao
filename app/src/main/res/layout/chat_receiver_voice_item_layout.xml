<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp"
    xmlns:tools="http://schemas.android.com/tools">


    <TextView
        android:id="@+id/userChatTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingLeft="32dp"
        android:paddingTop="16dp"
        android:paddingBottom="16dp"
        android:paddingRight="16dp"
        tools:text="12~"
        android:drawableRight="@drawable/receiver_voice_amin"
        android:drawablePadding="32dp"
        android:textSize="14sp"
        android:gravity="right"
        android:layout_marginTop="24dp"
        android:layout_marginLeft="24dp"
        android:layout_marginRight="48dp"
        android:background="@color/c_f2f4f8"
        android:textColor="@color/c_3c3c3c"/>

    <ImageView
        android:id="@+id/userAvatarImageView"
        android:layout_width="@dimen/chat_avatar_default_size"
        android:layout_height="@dimen/chat_avatar_default_size"
        android:background="@drawable/white_circle_background"
        android:padding="2dp"
        tools:src="@mipmap/fragment_personal_default_header_image"/>
</FrameLayout>